# api-worker

This folder contains the API Worker for Glide (simple starter files).

Files added:
- index.ts — minimal Cloudflare Worker handler for POST /process

Add further worker configuration (wrangler.toml, package.json) as needed for local development and deployment.
